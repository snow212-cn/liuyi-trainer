# Standards Assets

This directory stores the standards illustrations and related assets used by the
current APK build baseline.

When updating these files:

- keep the source record clear
- avoid replacing working assets without validating the app flow
- document any large asset refresh in the repository docs or PR
